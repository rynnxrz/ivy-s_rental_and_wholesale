export const SITE_CONFIG = {
    contact_email: process.env.NEXT_PUBLIC_CONTACT_EMAIL || 'ivy@example.com', // TODO: Load from app_settings in the future if needed, but this serves as the fallback/constant.
}
