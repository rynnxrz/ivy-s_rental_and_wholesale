# IVYJSTUDIO MVP 交接文档

**日期:** 2026-03-13
**版本:** v1.4 (Reservation Workflow + PDF Import)
**目标:** B2B 高级珠宝租赁与批发管理系统

---

## 1. 技术栈 (Tech Stack)
*   **框架:** Next.js 16 (App Router) + React 19
*   **样式:** Tailwind CSS v4 + Shadcn UI (Radix)
*   **数据库/后端:** Supabase (Postgres, Auth, Storage, Edge Functions)
*   **AI 引擎:** Google Gemini (Search Grounding + PDF Catalog Parsing)
*   **文档/图像处理:** `pdfjs-dist`, `sharp`, `react-signature-canvas`
*   **部署:** Vercel
*   **服务:** Resend (邮件), Cloudflare (DNS/CDN), Microsoft Clarity (用户行为分析)

---

## 2. 核心功能交付 (Completed Features)

### A. 前台门户 (Portal)
1.  **三路分流架构**:
    *   **Rental**: 基于日期的租赁画册，含实时库存冲突检测。
    *   **Wholesale**: 批发专区，全场 6 折，起订量 $2,000 校验，支持独立批发登录鉴权。
    *   **Archive**: 品牌归档展示，无交易功能。
2.  **租赁请求信息增强**:
    *   请求数据会保存 **Event Location**、地址快照，以及原始借用日期元数据。
    *   为后续管理员改期、合同渲染、付款确认提供数据基础。
3.  **付款确认入口**:
    *   新增 `/payment/[reservationId]`，自动跳转到该订单最新 Invoice 的付款确认页。
    *   新增 `/payment-confirmation/[invoiceId]`，客户可在线查看 Invoice、PDF、Loan Form，并完成数字签名与付款声明。
4.  **移动端与详情体验**:
    *   1:1 正方形留白构图，多图画廊。
    *   移动端吸底交互与安全区适配已保留。

### B. 后台管理 (Admin Dashboard)
1.  **订单生命周期重构 (Reservations)**:
    *   状态统一为四阶段:
        *   `Pending Request`
        *   `Upcoming`
        *   `Ongoing`
        *   `Past-loan`
    *   管理页按新状态拆分视图，并保留 **Archived** 独立筛选入口。
2.  **审批与账单工作流升级**:
    *   `Review & Invoice` 弹窗支持:
        *   确认借出/归还日期
        *   Event Location 预览
        *   Discount Percentage
        *   Deposit Override
        *   实时 Invoice 价格预览
    *   审批时会生成 Invoice、发送 Approval Email，并附带公开付款确认链接。
3.  **付款确认与合同闭环**:
    *   客户付款确认页支持:
        *   Invoice 金额拆解 (Subtotal / Discount / Deposit / Total Due)
        *   Loan Form 条款展示
        *   数字签名上传
        *   已确认付款与已阅读条款的双勾选确认
    *   签名文件会写入 Storage，并回写到 Reservation / Invoice。
4.  **Upcoming / Ongoing 阶段动作补强**:
    *   **Upcoming**:
        *   直接查看 Invoice PDF
        *   Resend Invoice Email
        *   Dispatch 发货通知（可附 Invoice）
    *   **Ongoing**:
        *   上传 Return Evidence
        *   `Confirm Return` 进入 `Past-loan`
    *   **Past-loan**:
        *   支持 Archive 归档
5.  **商品管理 (Inventory) 升级为 Taxonomy 模式**:
    *   `items` 新增:
        *   `line_type`: Mainline / Collaboration / Archive
        *   `character_family`: Orchid / Daffodil / Botanic Elegy / Oceanspine / Sea Passiflora / Uncategorized
    *   列表页现按 **Line Type Tabs + Character Family Grouping** 组织展示。
    *   支持 Character Group 维度的批量 `Mark Active / Retire All`。
    *   提供 `Run Taxonomy Backfill`，对历史数据自动回填分类维度。
6.  **AI Import Ecosystem 扩展到 PDF Catalog**:
    *   原有 URL Speed Scan 工作流保留。
    *   新增 **PDF Catalog Import**:
        *   PDF 上传至私有 Bucket
        *   Gemini 解析 Catalog 行数据
        *   自动写入 staging batch
        *   支持 `source_type`, `source_label`, `source_page`, `default_line_type`
        *   浏览器端渲染指定 PDF 页面后，调用 AI 进行商品图像定位与裁切
    *   Staging Review 支持编辑 `line_type`, `character_family`, `specs`, `source_page` 等字段。

### C. 通讯中心与 Invoice 能力
1.  **Invoice 价格模型升级**:
    *   `invoices` 新增:
        *   `subtotal_amount`
        *   `discount_percentage`
        *   `discount_amount`
        *   `deposit_amount`
    *   租赁价格由 Retail Value 和租期阶梯计算，支持折扣与押金拆分。
2.  **邮件能力升级**:
    *   Approval Email 现在可附带:
        *   最新 Invoice PDF
        *   付款确认页面链接
    *   Admin 可对 Upcoming 订单执行 Resend Invoice。
3.  **PDF Invoice 复用能力**:
    *   Admin 可从 Reservation 页面直接查看 PDF。
    *   Public payment-confirmation 页面也可内嵌/打开同一份 Invoice PDF。

---

## 3. 数据库与维护 (Maintenance)

### 数据库变更
本次版本核心依赖以下 Migration:

*   `00039_reservation_status_invoice_pricing.sql`
    *   Reservation 新状态模型
    *   Invoice 金额拆分字段
    *   `payment_signature_url`
*   `00040_reservation_invoice_overrides.sql`
    *   Reservation 级别 `discount_percent` / `deposit_override`
*   `00041_item_taxonomy_dimensions.sql`
    *   `items.line_type`
    *   `items.character_family`
*   `00042_pdf_import_taxonomy.sql`
    *   `staging_imports` PDF 元数据
    *   `staging_items` taxonomy/specs/source_page
    *   `import_documents` bucket
*   `00043_digital_loan_form_metadata.sql`
    *   `original_start_date`
    *   `original_end_date`
    *   `event_location`

> 建议后续所有 Schema 变更都以 `supabase/migrations/*.sql` 为准进行版本管理；变更完成后必须同步类型。

```bash
npm run update-types
```

### Storage 约定
*   **主商品图 Bucket:** `rental_items`
*   **商品图 Path:** `/items/{filename}`
*   **PDF 原件 Bucket:** `import_documents`
*   **PDF Preview Crop:** `rental_items/import-previews/...`
*   **签名文件 Path:** `rental_items/payment-signatures/...`
*   **Invoice PDF Path:** `rental_items/invoices/{invoiceId}/latest.pdf`

### 关键配置 (Environment Variables)
*   `NEXT_PUBLIC_SUPABASE_URL`: Supabase 项目 URL
*   `NEXT_PUBLIC_SUPABASE_ANON_KEY`: 新格式 `sb_publishable_...`
*   `SUPABASE_SERVICE_ROLE_KEY`: 新格式 `sb_secret_...`
*   `RESEND_API_KEY`: 邮件服务
*   `GOOGLE_AI_API_KEY` / `GEMINI_API_KEY`: Gemini AI 调用
*   `NEXT_PUBLIC_APP_URL` 或 `NEXT_PUBLIC_SITE_URL`: Approval Email / Payment Confirmation Link 的公开地址生成依赖

### 运维备注
*   若未配置 `NEXT_PUBLIC_APP_URL` / `NEXT_PUBLIC_SITE_URL`，Approval Email 仍可发送，但付款确认链接可能缺失。
*   PDF 导入依赖 `import_documents` bucket 与 Gemini File API。
*   Reservation Archive 在旧环境下支持回退到 `admin_notes` 标记方案，以兼容未完全同步的状态枚举。
